<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }
    body {
      font-family: Arial, sans-serif;
      font-size: 11px;
      line-height: 1.4;
      margin: 0;
      padding: 32px 0;
      min-height: 100%;
    }
    .page-inner {
      max-width: 170mm;
      width: 100%;
      margin: 0 auto;
    }
    .company-header {
      text-align: center;
      margin-bottom: 30px;
      padding-bottom: 15px;
      border-bottom: 3px solid #000;
    }
    .company-name {
      font-size: 16px;
      font-weight: bold;
      margin-bottom: 5px;
    }
    .invoice-header {
      display: grid;
      grid-template-columns: 2fr 1fr;
      gap: 20px;
      margin-bottom: 30px;
    }
    .invoice-title {
      font-size: 24px;
      font-weight: bold;
      text-align: right;
    }
    .invoice-info {
      text-align: right;
    }
    .section {
      border: 2px solid #000;
      padding: 15px;
      margin-bottom: 20px;
    }
    .section-title {
      font-weight: bold;
      font-size: 12px;
      margin-bottom: 10px;
      text-decoration: underline;
    }
    .info-row {
      margin-bottom: 5px;
    }
    .label {
      font-weight: bold;
      display: inline-block;
    }
    table {
      width: 100%;
      border-collapse: collapse;
      margin: 20px 0;
    }
    th {
      background-color: #f0f0f0;
      border: 2px solid #000;
      padding: 10px;
      text-align: left;
      font-weight: bold;
    }
    td {
      border: 2px solid #000;
      padding: 10px;
    }
    .total-row {
      font-weight: bold;
      font-size: 13px;
    }
    .bank-info {
      background-color: #f9f9f9;
      padding: 15px;
      border: 2px solid #000;
      margin-top: 20px;
    }
    .bank-title {
      font-weight: bold;
      font-size: 12px;
      margin-bottom: 10px;
      text-align: center;
      text-transform: uppercase;
    }
    .footer-note {
      margin-top: 30px;
      padding: 10px;
      font-size: 10px;
      font-style: italic;
    }
  </style>
</head>
<body>
  <div class="page-inner">
  <div class="company-header">
    <div class="company-name"><%= consigneeName %></div>
    <div><%= consigneeAddress %></div>
  </div>

  <div class="invoice-header">
    <div>
      <div class="section-title">TO:</div>
      <div style="font-weight: bold; font-size: 13px;"><%= brazilBiName %></div>
      <div style="margin-top: 5px;">
        <div>IDENT CNPJ: <%= brazilBiCnpj %></div>
      </div>
    </div>
    <div>
      <div class="invoice-title">INVOICE</div>
      <div class="invoice-info" style="margin-top: 10px;">
        <div><strong>Invoice Nº</strong> <%= invoiceNumber %></div>
        <div style="margin-top: 5px;"><strong>Date:</strong> <%= invoiceDate %></div>
      </div>
    </div>
  </div>

  <div class="section">
    <div style="font-weight: bold; margin-bottom: 10px;">
      This Invoice is intended for the payment of international maritime freight for the following BLs (Bill of Ladings):
    </div>
  </div>

  <table>
    <thead>
      <tr>
        <th style="width: 60%;">BL NUMBER</th>
        <th style="width: 40%; text-align: right;">AMOUNT</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td><%= blNumber %></td>
        <td style="text-align: right;"><%= currency %> <%= freightValue %></td>
      </tr>
      <tr class="total-row">
        <td>TOTAL</td>
        <td style="text-align: right;"><%= currency %> <%= freightValue %></td>
      </tr>
    </tbody>
  </table>

  <div class="bank-info">
    <div class="bank-title">The payment must be done in the beneficiary's account. Please check the bank and account information below:</div>
    
    <div style="margin-top: 15px;">
      <div class="info-row">
        <span class="label">BENEFICIARY BANK:</span> <%= beneficiaryBank %>
      </div>
      <div class="info-row">
        <span class="label">BANK ADD:</span> <%= beneficiaryAddress %>
      </div>
      <div class="info-row">
        <span class="label">SWIFT CODE:</span> <%= swiftCode %>
      </div>
      <div class="info-row">
        <span class="label">BENEFICIARY ACCOUNT:</span> <%= accountNumber %>
      </div>
      <div class="info-row">
        <span class="label">US Correspondent:</span> <%= correspondent %>
      </div>
      <div class="info-row">
        <span class="label">Routing #:</span> <%= routingNumber %> / MEMO FIELD: <%= beneficiaryBank %> / <%= routingNumber %>
      </div>
    </div>
  </div>

  <div class="footer-note">
    <div style="text-align: center; font-weight: bold;">
      Invoice Number: <%= invoiceNumber %>
    </div>
  </div>
  </div>
</body>
</html>

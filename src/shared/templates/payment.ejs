<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }
    body {
      font-family: Arial, sans-serif;
      font-size: 11px;
      line-height: 1.4;
      margin: 0;
      padding: 32px 0;
      min-height: 100%;
    }
    .page-inner {
      max-width: 170mm;
      width: 100%;
      margin: 0 auto;
    }
    .header {
      text-align: center;
      margin-bottom: 30px;
    }
    .header h1 {
      font-size: 18px;
      font-weight: bold;
      margin-bottom: 5px;
    }
    .section {
      border: 2px solid #000;
      padding: 15px;
      margin-bottom: 15px;
    }
    .section-title {
      font-weight: bold;
      font-size: 12px;
      margin-bottom: 10px;
      text-decoration: underline;
    }
    .info-row {
      margin-bottom: 8px;
    }
    .label {
      font-weight: bold;
      display: inline-block;
      min-width: 150px;
    }
    .grid {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 20px;
    }
    .company-header {
      text-align: center;
      margin-bottom: 20px;
      padding-bottom: 10px;
      border-bottom: 2px solid #000;
    }
    .company-name {
      font-size: 14px;
      font-weight: bold;
    }
    .checkbox-row {
      margin: 10px 0;
    }
    .checkbox {
      display: inline-block;
      width: 15px;
      height: 15px;
      border: 1px solid #000;
      margin-right: 5px;
      text-align: center;
      line-height: 15px;
    }
    .total-section {
      text-align: right;
      font-size: 14px;
      font-weight: bold;
      margin-top: 20px;
      padding-top: 10px;
      border-top: 2px solid #000;
    }
    .footer {
      text-align: right;
      margin-top: 30px;
      font-size: 10px;
    }
  </style>
</head>
<body>
  <div class="page-inner">
  <div class="company-header">
    <div class="company-name"><%= brazilBiName %></div>
    <div><%= brazilBiAddress %></div>
    <div>CNPJ: <%= brazilBiCnpj %></div>
  </div>

  <div class="header">
    <h1>PAGAMENTO DE FRETE</h1>
    <div style="margin-top: 10px;">
      <strong>CIRCULAR:</strong> <%= circular %>
    </div>
  </div>

  <div class="grid">
    <div class="section">
      <div class="section-title">DADOS DO REMETENTE</div>
      <div class="info-row">
        <span class="label"><%= brazilBiName %></span>
      </div>
      <div class="info-row">
        <span class="label">A/C NAME:</span> <%= consigneeName %>
      </div>
      <div class="info-row">
        <div><%= brazilBiAddress %></div>
        <div>CEP: <%= brazilBiCep %></div>
      </div>
      <div class="info-row">
        <span class="label">CNPJ:</span> <%= brazilBiCnpj %>
      </div>
    </div>

    <div class="section">
      <div class="section-title">DADOS DO RECEBEDOR NO EXTERIOR</div>
      <div class="info-row">
        <div style="font-weight: bold;"><%= consigneeName %></div>
        <div><%= consigneeAddress %></div>
      </div>
      <div style="margin-top: 10px;">
        <div class="info-row">
          <span class="label">BENEFICIARY BANK:</span> <%= beneficiaryBank %>
        </div>
        <div class="info-row">
          <span class="label">BANK ADD:</span> <%= beneficiaryAddress %>
        </div>
        <div class="info-row">
          <span class="label">SWIFT CODE:</span> <%= swiftCode %>
        </div>
        <div class="info-row">
          <span class="label">BENEFICIARY ACCOUNT:</span> <%= accountNumber %>
        </div>
        <div class="info-row">
          <span class="label">US Correspondent:</span> <%= correspondent %>
        </div>
        <div class="info-row">
          <span class="label">Routing #:</span> <%= routingNumber %> / MEMO FIELD: <%= beneficiaryBank %> / <%= routingNumber %>
        </div>
      </div>
    </div>
  </div>

  <div class="section">
    <div class="section-title">MODALIDADE</div>
    <div class="checkbox-row">
      <span class="checkbox"> </span> AÉREO
      <span class="checkbox" style="margin-left: 30px;">X</span> MARÍTIMO
    </div>
  </div>

  <div class="section">
    <table style="width: 100%; border-collapse: collapse;">
      <tr style="background-color: #f0f0f0;">
        <th style="border: 1px solid #000; padding: 8px; text-align: left;">Nº B/L - NAVIO/VIAGEM</th>
        <th style="border: 1px solid #000; padding: 8px; text-align: left;">INCOTERM</th>
        <th style="border: 1px solid #000; padding: 8px; text-align: right;">VALOR</th>
      </tr>
      <tr>
        <td style="border: 1px solid #000; padding: 8px;"><%= blNumber %> - <%= vessel %></td>
        <td style="border: 1px solid #000; padding: 8px;"><%= incoterm %></td>
        <td style="border: 1px solid #000; padding: 8px; text-align: right;"><%= currency %> <%= freightValue %></td>
      </tr>
    </table>
  </div>

  <div class="total-section">
    TOTAL: <%= currency %> <%= freightValue %>
  </div>

  <div class="footer">
    <div><%= brazilBiName.split(' ')[0] %>, <%= paymentDate %></div>
    <div style="margin-top: 5px;">CNPJ: <%= brazilBiCnpj %></div>
  </div>
  </div>
</body>
</html>
